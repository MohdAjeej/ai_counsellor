# Copy to .env for local dev. For Railway, set these in the dashboard: Service → Variables.
# Do NOT commit .env (it contains secrets).

DATABASE_URL=postgresql://user:password@localhost:5432/ai_counsellor
SECRET_KEY=your-long-random-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
GEMINI_API_KEY=your-gemini-api-key-from-google-ai-studio
GEMINI_MODEL=gemini-2.0-flash
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
